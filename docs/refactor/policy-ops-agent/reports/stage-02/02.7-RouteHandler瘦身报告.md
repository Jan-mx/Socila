# 步骤 02.7 Route Handler 协议适配层报告

- 需求：CORE-FR-006（验证：直连DB扫描、API契约；完成条件：Route 无领域逻辑）
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 交付物

| 交付物 | 说明 |
| --- | --- |
| 模块仓储访问器 | rules/planning/conversation/publishing 模块 `application/index.ts` 进程级单例（`rulesReads`/`rulesWrites`/…） |
| 22 个 Route Handler 迁移 | 全部 `@/lib/db/queries` 直接导入替换为模块仓储访问器调用（codemod 批量执行 + 人工复核） |
| 2 个直连 db 路由消除 | `admin/cases`（案例搜索分页迁至 `planningReads.searchCases`）、`admin/rule-sets/[id]`（最新草稿查询迁至 `rulesReads.getLatestRuleSetVersion`） |
| 服务端组件迁移 | `(client)/cases/page.tsx` 改用 `planningReads.listShowcaseCases` |
| 归属校验上移 | `plan/[id]`（decideOwnership + 404 防枚举语义保持）、`conversations/[conversationId]`（deleteOwnedConversation 用例，403/404 语义保持） |
| 计算入口统一 | `/api/plan/compute` 与 AI 工具 `computePlan` 直用 ComputePlanUseCase（不再经过 plan-service 垫片） |
| AC-001 自动化门禁 | `src/app/__tests__/route-dependencies.test.ts`：src/app 下禁止导入 `@/lib/db`、`@/lib/db/schema`、`@/lib/db/queries` |

API 契约：所有替换为等价仓储调用，URL、成功响应字段、错误状态均未改变（黄金测试 + 契约测试 + 构建全部通过）。

## 2. 验证证据

| 验证 | 结果 |
| --- | --- |
| 直连DB扫描（自动化测试） | src/app 下 0 处 `@/lib/db`、`@/lib/db/schema`、`@/lib/db/queries` 导入 |
| 直连DB扫描（node 实测复核） | total hits: 0 |
| `npm test` 全套 | 23 文件：21 通过 + 2 skipped；142 通过 + 15 skipped |
| `npx eslint src` / `npx tsc --noEmit` / `npm run build` | 0 / 0 / 0 |

## 3. 结论

CORE-FR-006 满足：Route Handler 只剩认证、解析、用例/仓储调用与响应映射，数据库触达全部经领域模块，且该约束有自动化测试守门。步骤02.7 完成。
