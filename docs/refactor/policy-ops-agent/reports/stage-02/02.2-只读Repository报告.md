# 步骤 02.2 只读 Repository 报告

- 需求：CORE-FR-004（验证：新旧结果对比；完成条件：覆盖全部只读调用）
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`
- 说明：**执行顺序调整**——02.4 的驱动切换（pg Pool）提前到本步骤完成。原因：02.2 的验证路径（seed 演练库 + 新旧对账）依赖本地 PostgreSQL 连接，而旧 neon-http 驱动只能走 websocket，无法到达演练库。依赖顺序所致，两步的验证均在本报告与 02.4 报告中留痕。

## 1. 交付物

### 1.1 只读端口（application/ports.ts）与 Drizzle 实现（infrastructure/drizzle/）

| 模块 | 端口 | 覆盖 queries.ts 只读函数 |
| --- | --- | --- |
| rules | `RulesReadRepository` | getEffectiveRules、getRule、listRules、listRuleVersions、getEffectiveParams、listParams、getRuleSet、listRuleSets、getWorkflow、listTests、getTest、countRules、countParams、countTests（14个） |
| policy | `PolicyReadRepository` | getLatestPolicyPackVersion |
| publishing | `PublishReadRepository` | listPublishes |
| planning | `PlanningReadRepository` | getPlan、listPlans、listCases、listShowcaseCases、countShowcaseCases（5个） |
| conversation | `ConversationReadRepository` | getConversation、listConversations |

只读函数共 23 个，**全部覆盖**（identity/jurisdiction/audit/agent-integration 当前无只读需求）。

### 1.2 驱动切换（原 02.4 交付，提前实施）

- `src/lib/db/index.ts` 重写：`pg.Pool`（max/idle/connection 超时经 `DATABASE_POOL_MAX` 等环境变量可配）+ `drizzle-orm/node-postgres`；保持惰性代理——模块导入与构建不建连接（CORE-AC-006）。
- 新增 `getPool()`（健康检查/指标）与 `closeDatabase()`（进程优雅关闭，PRD §7）。
- `@neondatabase/serverless` 已从 dependencies 移除；`scripts/generate-showcase-cases.ts` 同步切至 pg Pool（成功路径显式 `pool.end()`）。
- 同请求不混用两个写连接：全仓库唯一 db 实例（PRD §14）。

### 1.3 新旧结果对比测试

- `src/server/modules/__tests__/repository-parity.test.ts`：12 项对比，覆盖全部 23 个只读函数（含空表路径与 fixture 路径）。
- 门控：仅当 `SSP_TEST_DATABASE_URL` 设置时运行（默认套件保持无 DB 依赖）；演练库准备：`DATABASE_URL=<drill> npm run db:migrate && npm run seed`（seed 含 500 条回归测试 + 规则/参数/规则集/工作流/案例）。

## 2. 验证证据

| 验证 | 结果 |
| --- | --- |
| 新旧对账（seed 演练库，12 项对比） | 12/12 通过——旧 queries.ts 与新仓储在同一演练库上逐项深比较一致 |
| 默认套件（parity 跳过） | 19 文件：18 通过 + 1 skipped；126 通过 + 12 skipped |
| `npx eslint src` / `npx tsc --noEmit` | 0 / 0 |
| `npm run build` | 0（pg Pool 惰性初始化，构建期无连接） |
| seed 于演练库 | 退出码 0（切换驱动后本地 seed 可用） |

## 3. 结论

CORE-FR-004（只读部分）与 CORE-FR-008 的连接层满足：全部只读调用已有模块仓储实现并通过对账验证；驱动切换完成且无 Neon 运行时依赖。步骤02.2 完成（含 02.4 前半），02.3 起迁移写路径。
