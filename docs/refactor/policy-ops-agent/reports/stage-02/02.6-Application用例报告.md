# 步骤 02.6 Application 用例报告

- 需求：CORE-FR-005/010
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 交付物

| 交付物 | 位置 | 说明 |
| --- | --- | --- |
| ComputePlanUseCase | `src/server/modules/planning/application/compute-plan.use-case.ts` | 编排+富化+落库收敛为 planning 用例；引擎/落库端口可注入（无框架单测）；落库绑定 ownerUserId/sessionId（CORE-FR-009）。旧 `src/lib/engine/plan-service.ts` 转为垫片转发（02.9 删除） |
| Conversation 用例 | `src/server/modules/conversation/application/conversation.use-case.ts` | 归属受控的 get/update/delete/list；legacy 会话拒绝语义与现状一致；归属判定复用 identity 领域模型 |
| Agent 集成端口 | `src/server/modules/agent-integration/application/ports.ts` | `PolicyContextPort`（阶段03实现）+ `DraftMaterializationPort`（阶段06实现）——只定义端口，Agent 只能经端口读写 |
| 发布用例 | `src/server/modules/publishing/application/publish-use-case.ts` | `createPublishingUseCase`（recordPublish 必填 actor 留痕 + listHistory）；promote/rollback 状态机在 02.7 协议适配期保持经 publish-service |
| 用例单元测试 | `src/server/modules/__tests__/use-cases.test.ts` | Fake 端口：规划落库/归属绑定/persist 开关、会话归属矩阵、发布留痕——**不启动 Next、不连数据库** |

事务/权限在用例层：规划落库经 `withTransaction` 能力（单插入原子）+ owner 绑定；会话/方案归属判定复用 identity 领域模型（decideOwnership）；发布记录强制 actor 归因。

## 2. 验证证据

| 验证 | 结果 |
| --- | --- |
| 用例单元测试（Fake 端口） | 9 项全过 |
| `npm test` 全套 | 22 文件：20 通过 + 2 skipped；141 通过 + 15 skipped |
| `npx eslint src` / `npx tsc --noEmit` | 0 / 0 |

## 3. 结论

CORE-FR-005/010 满足：规划/会话/发布用例与 Agent 端口就位，事务与权限收敛在用例层且可无框架测试。步骤02.6 完成。
