# 步骤 02.1 领域骨架报告

- 需求：CORE-FR-001～003
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 交付物

| 交付物 | 位置 |
| --- | --- |
| 九个领域模块目录（identity / jurisdiction / policy / rules / planning / conversation / publishing / audit / agent-integration） | `src/server/modules/<module>/{domain,application,infrastructure,contracts}` |
| 依赖规则文档 | `src/server/modules/README.md`（依赖方向图 + 模块职责表 + 扫描说明） |
| 各模块边界 README | `src/server/modules/<module>/README.md`（层职责、禁用导入、模块间依赖约定） |
| 依赖扫描测试 | `src/server/modules/__tests__/module-boundaries.test.ts` |

## 2. 依赖规则（三层）

1. **全层禁框架**：`src/server/modules` 下所有层禁止导入 `next/*`、`react`、`react-dom`、`@ai-sdk/*`、`ai`——领域模块与框架解耦，Route Handler 是唯一框架接触面（CORE-FR-003）。
2. **domain 禁数据库**：`domain/` 额外禁止 `drizzle-orm`、`pg`、`src/lib/db`——领域纯净性，仓储依赖经端口倒置。
3. **扫描即测试**：扫描器解析全部源文件的 import 说明符（static/import()/require），违规即测试失败；模块清单变更须同步断言。

## 3. 验证证据

| 验证 | 结果 |
| --- | --- |
| 边界扫描测试（3 项断言：模块清单、全层禁框架、domain 禁DB） | 通过（修复初次发现的清单枚举含 `__tests__` 问题） |
| `npm test` 全套 | 18 文件 / 126 测试全通过 |
| `npx eslint src` / `npx tsc --noEmit` | 0 / 0 |

## 4. 结论

CORE-FR-001～003 满足：模块目录、依赖规则、README 与依赖扫描就位。骨架为空目录（.gitkeep），代码迁移在 02.2 起逐域进行。步骤02.1 完成。
