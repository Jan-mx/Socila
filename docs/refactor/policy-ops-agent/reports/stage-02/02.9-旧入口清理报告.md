# 步骤 02.9 旧入口清理报告

- 需求：CORE-FR-004（清理被替代的查询/导入）
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 删除项

| 删除 | 理由 |
| --- | --- |
| `src/lib/db/queries.ts`（547 行，41 函数，27 调用方） | 全部只读/写函数已由五模块仓储覆盖（02.2/02.3），src/lib 内最后三个引用方（orchestrator、test-runner、publish-service）已重指向模块仓储 |
| `src/lib/engine/plan-service.ts`（垫片） | 调用方（/api/plan/compute、AI 工具 computePlan）已直用 ComputePlanUseCase |
| `src/server/modules/__tests__/repository-parity.test.ts` | 对账目标（旧 queries）已删除；对账证据保留于 02.2/02.3 报告与 Git 历史；CI repository-gates 命令同步更新 |

保留：`scripts/*`（seed/showcase 等 dev 工具直接使用 db/schema，属基础设施工具而非应用入口）；`src/lib/db/schema.ts` 仍是唯一声明式 Schema 源。

## 2. 验证证据

| 验证 | 结果 |
| --- | --- |
| 引用扫描 `grep -r "db/queries" src scripts` | 0 命中（仅 route-dependencies 门禁测试中的禁用清单字符串） |
| `npm test` | 23 文件：22 通过 + 1 skipped；146 通过 |
| eslint / tsc | 0 / 0 |

## 3. 结论

DoD"集中式查询入口不再承担跨域职责，已替代导入被清理"满足。步骤02.9 完成。
