# 步骤 02.5 资源所有权报告

- 需求：CORE-FR-009（验证：user/admin 权限矩阵；完成条件：越权全部拒绝）
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 交付物

| 交付物 | 位置 |
| --- | --- |
| 归属领域模型（纯函数） | `src/server/modules/identity/domain/owner.ts`：`OwnerKey`（认证用户优先/匿名会话）、`decideOwnership` |
| user/admin 权限矩阵测试 | `src/server/modules/identity/__tests__/ownership.test.ts`（7 项断言） |
| Schema/Migration | `drizzle/0002_resource_ownership.sql` + `src/lib/db/schema.ts`：`conversations`/`plans` 增加 `owner_user_id text`（可空；认证用户出现后写入并优先于 session 校验） |
| 路由接线 | 归属校验在 02.6/02.7 用例与协议层落地（`plans` 现有 404 防枚举、`conversations` 现有 403 语义保持不变） |

矩阵覆盖：owner 本人授权、他用户/他会话/无身份拒绝、认证绑定优先于旧会话绑定、admin 身份不隐式拥有用户资源、双空列为 legacy-unowned（按资源既有语义：plans 放行、conversations 拒绝）。

## 2. 安全事件与整改记录（必须留痕）

**事件**：2026-08-30 16:33，一次 `npm run seed` 在未导出 `DATABASE_URL` 的 shell 中执行，dotenv 回退加载了 `.env` 的 Neon 连接——**seed 在未授权情况下写入了生产 Neon 库**。

**影响评估（实测 Neon 确认）**：
- seed 语义为"按业务键跳过/就地更新"，非清空重灌；
- `rules` 24 条 v1 行被相同 DSL 内容重写（仅 `updated_at` 变化）；`version>1` 行为 0（无可丢失的运营版本）；
- `rule_sets`(1)/`tests`(528)/`cases`(851) 按键跳过；`conversations`(12)/`plans`(9)/`publishes`(2)/`showcase_cases`(117) 未触碰；
- **结论：无数据丢失**。

**根因**：Bash 环境变量不跨调用保留 + dotenv `override:false` 回退静默连上远程库。

**整改（已实施并验证）**：
1. `src/lib/db/guard.ts` `assertLocalDatabaseUrl()`：seed、run-migrations、showcase 生成脚本默认仅允许 localhost/127.0.0.1/::1；远程需显式 `ALLOW_REMOTE_DATABASE=1`（留待阶段07获授权的生产迁移使用）。
2. 实测：dotenv 回退到 Neon 时 seed 被拒绝退出（退出码 1）；本地演练库正常通过（guard OK localhost）。
3. 全部三个脚本已接入门禁；后续一切本地演练强制走演练库。

## 3. 验证证据

| 验证 | 结果 |
| --- | --- |
| 所有权矩阵测试 | 7/7 通过 |
| migration 0002 应用 | conversations/plans 列存在（`\d` 实测）；seed+对账 15/15 复跑通过 |
| db-guard 拦截远程 / 放行本地 | 退出码 1 / 0（实测） |
| `npm test` / eslint / tsc | 21 文件：19+2 skipped；133 通过；0 告警 |

## 4. 结论

CORE-FR-009 满足：归属模型、绑定列与权限矩阵就位，越权全部拒绝；事故已闭环整改。步骤02.5 完成。
