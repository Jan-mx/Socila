# 步骤 02.4 本地 PostgreSQL 驱动切换报告

- 需求：CORE-FR-008（验证：连接、事务、关闭、构建测试；完成条件：无 Neon 运行时依赖）
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`
- 说明：本步骤的实现实体（pg Pool + node-postgres）在 02.2 执行中因验证依赖提前完成；本报告汇总其全部验证证据。

## 1. 交付物与证据

| 要求 | 实现 | 证据 |
| --- | --- | --- |
| `pg` + `drizzle-orm/node-postgres` 受控 Pool | `src/lib/db/index.ts`：单例 Pool（`DATABASE_POOL_MAX`/`DATABASE_IDLE_TIMEOUT_MS`/`DATABASE_CONNECTION_TIMEOUT_MS` 可配）+ drizzle 实例 | 仓储集成测试 15/15 在 pg 连接上通过 |
| 模块导入不建连接（CORE-AC-006 前置） | 保持惰性 Proxy——首次属性访问才创建 Pool | `npm run build`（无 DB 网络）退出码 0 |
| 连接测试 | parity 对账 12 项经 Pool 查询 | 通过 |
| 事务测试 | `withTransaction` 回滚用例 | 通过（回滚后行数不变） |
| 关闭行为 | `closeDatabase()`（pool.end）+ `getPool()` 指标访问 | 导出就绪；进程关闭接线于部署阶段07 |
| 无 Neon 运行时依赖 | `@neondatabase/serverless` 已 npm uninstall；全源码 grep 无真实导入（仅文档注释提及） | `npm ls @neondatabase/serverless` 空 |
| 驱动行为与基线一致（CORE-AC-003 前置） | 同库 seed + 旧函数/新仓储双路径结果一致 | 12/12 对账一致 |

## 2. CI

`repository-gates` job 在 CI 的 PostgreSQL 17 服务上持续执行 migration/seed/仓储测试（见 02.3 报告）。

## 3. 结论

CORE-FR-008 满足：本地 PostgreSQL 标准驱动切换完成，无 Neon 运行时依赖，连接/事务/构建验证通过。步骤02.4 完成（实现实体在 02.2 落地）。
