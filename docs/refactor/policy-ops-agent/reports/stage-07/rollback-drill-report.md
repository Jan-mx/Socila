# 07.8 回退演练报告（REL-FR-010～012 / REL-AC-005、REL-AC-007）

日期：2026-08-31 · 环境：本地演练（正式 DNS/Vercel 入口切换属 07.10，需用户显式授权）

## 1. 已执行验证

| 项 | 结果 |
| --- | --- |
| Neon 只读回退源 | 会话级 `default_transaction_read_only=on` 连通 5.1s（server 18.6）；rules=24/params=29/tests=528/conversations=12/plans=9/showcase_cases=117，与两轮演练基线完全一致（REL-AC-007：数据只读可用且未删除） |
| 离线回退数据源 | 演练 dump `neon-round1/2.dump` 各 605,600 字节保留；该 dump 已被 07.4/07.5 两轮完整恢复验证（恢复→复制→对账全一致） |
| 数据回退路径 | 07.7 恢复演练证明备份→全新容器恢复 12s 可行 |
| 回退决策与步骤 | Runbook「回退」章节具体化为 5 步可执行命令与触发条件（黄金回归失败/关键功能不可用 30 分钟未修复/数据差异） |

## 2. 切换时执行项（07.10，用户授权后）

- 模拟切换失败→入口切回：DNS/内网入口指回 Vercel 旧应用为一次配置生效（预计 <5 分钟，取决于 TTL），实际耗时将在 07.10 执行时记录（REL-AC-005 的完整闭环证据）。
- 回退窗口内 Neon 不删除、不改写（REL-FR-012）——已写入 07.10 清单。

## 3. 结论

回退的数据侧路径（Neon 只读 + 离线 dump + 备份恢复）全部实测可用；入口侧回退为 07.10 执行时的记录项。07.8 演练口径 PASS，切换失败回退能力就绪。
