# 步骤 01.1 版本与命令基线报告

- 需求：FND-FR-001
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`（HEAD：`cba8cdc`）
- 执行环境：Windows 10.0.22631 x64 / Git Bash

## 1. 版本清单

| 组件 | 版本 | 来源 |
| --- | --- | --- |
| Node.js | v22.23.1 | `node --version` |
| npm | 11.7.0 | `npm --version` |
| Next.js | 16.2.9 | `package.json` |
| React / React DOM | 19.2.3 | `package.json` |
| TypeScript | ^5 | `package.json`（devDependencies） |
| ESLint | ^9（eslint-config-next 16.1.6） | `package.json` |
| Vitest | ^3.2.4 | `package.json` |
| Drizzle ORM / Kit | ^0.45.2 / ^0.31.9 | `package.json` |
| Zod | ^4.3.6 | `package.json` |
| 数据库驱动（现状） | `@neondatabase/serverless` ^1.0.2 | `package.json`（阶段02按CORE-FR-008切换为`pg`） |
| Python / uv（阶段04起） | 3.11.9 / 0.12.7 | `python --version` / `uv --version` |
| Docker CLI / Compose（阶段01.3起） | 29.4.0 / v5.1.2 | `docker --version` |
| PostgreSQL | 演练库 17.11（`postgres:17-alpine`，01.3 实测 `show server_version`）；Neon 线上库 18.6（对账记录） | 步骤01.3 回填 |

## 2. 命令基线（真实退出码）

| 命令 | 退出码 | 结果摘要 |
| --- | --- | --- |
| `npm test` | 0 | 15 个测试文件、112 个测试全部通过（2.11s） |
| `npx eslint src` | 0 | 无输出（无告警/错误） |
| `npx tsc --noEmit` | 0 | 无输出（类型检查通过） |
| `npm run build` | 0 | Next.js 16 生产构建成功（29 条 API 路由 + 页面全部生成） |

- 记录方式：`set -o pipefail` 下运行并捕获管道内真实退出码（首次无 pipefail 的运行亦全部通过，结果一致）。
- 时间：2026-08-30 14:58~15:01（本地时间）。

## 3. 与文档基线的一致性

- `progress.md`/`documentation-acceptance-report.md` 记录的基线为"15文件、112测试、四命令退出码0"——本次新鲜执行与之完全一致，无漂移。
- 本报告与四条命令的退出码构成阶段01后续步骤（01.2 黄金结果、01.3 migration、阶段02行为对比）的参照基线。

## 4. 环境事实登记

- 本机未检出 PostgreSQL 安装/Windows 服务/5432 监听（标准安装目录、`where psql`、服务列表、端口均已探测）。
- Docker Desktop 已安装但守护进程未启动；步骤01.3 将启动 Docker 并以 PostgreSQL 17 容器作为空库迁移演练库（见 ADR-0001 与 progress.md 环境事实）。

## 5. 结论

FND-FR-001 满足：版本与命令基线已记录、可重复验证、与既有证据一致。步骤01.1 完成。
