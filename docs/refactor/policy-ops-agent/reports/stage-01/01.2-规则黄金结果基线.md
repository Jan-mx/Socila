# 步骤 01.2 规则黄金结果基线报告

- 需求：FND-FR-003
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`（HEAD：`cba8cdc` + 本次步骤改动）

## 1. 黄金语料来源

| 组成 | 位置 | 规模 |
| --- | --- | --- |
| 规则 | `dsl/ssp_dsl_v1/rules/*.json` | 24 条（R-010～R-900） |
| 参数包 | `dsl/ssp_dsl_v1/params/policy_params_shanghai_base.json` | 标量 + 表参数（扁平化≥20） |
| 示例用例 | `dsl/ssp_dsl_v1/tests/rule_examples_as_tests.json` | 28 条 hand-authored 示例 |
| 规则集 | `dsl/ssp_dsl_v1/rule_sets/rule_set_shanghai_plan_v1.json` | 声明执行顺序 |

执行路径：`runTestSuite`（纯内存，`orchestrateInMemory`/`executeSingleRuleInMemory`），全程不依赖数据库、网络与模型。

## 2. 确定性保障（动态时间显式传入）

引擎时间源排查结论（全量 grep）：

| 位置 | 性质 | 处置 |
| --- | --- | --- |
| `orchestrator.ts:170`（内存编排缺省日期） | `new Date()` 回退 | **本次修复**：`runTestCase`/`runTestSuite` 新增可选 `asOfDate` 透传；快照固定传 `2026-01-01`（向后兼容，现有调用方不变） |
| `executor.ts:42,59`（trace 时间戳） | `Date.now()` | 快照中剔除 `timestamp` 字段 |
| `builtins.ts:158` | 纯 UTC 日历计算 | 确定性，无需处理 |
| `orchestrator.ts:50` / `test-runner.ts:257`（DB 路径缺省日期） | 生产路径 | 不在本快照范围；生产按入参 `as_of_date` |

## 3. 交付物

- **测试夹具**：`src/lib/engine/__tests__/golden-fixtures.ts`（语料加载、固定基准日期、trace 时间戳剔除、键序稳定序列化）。
- **快照基线**：`src/lib/engine/__tests__/fixtures/golden-snapshot.json`——28 条用例的完整实际输出（user/calc/plan/trace），32KB，已提交仓库。
- **快照回归测试**：`src/lib/engine/__tests__/golden-snapshot.test.ts`——两条断言：
  1. 全新加载的规则/参数重复执行两次，输出零漂移；
  2. 输出与提交的基线快照逐条一致（含语料清单一致）。
- **重新生成路径**：`WRITE_GOLDEN_SNAPSHOT=1 npx vitest run src/lib/engine/__tests__/golden-snapshot.test.ts`（输出显式提醒人工审阅 diff；禁止静默改期望）。
- 与既有 `golden.test.ts` 的关系：不改动原测试；快照在其"expected 字段级匹配"之上固化"完整实际输出"。

## 4. 已知偏差（登记清单，与 `golden.test.ts` KNOWN_DIVERGENCES 一致）

| 示例 | 根因（已核对，非引擎回归） |
| --- | --- |
| 退休年份2036 -> 19年（示例表） | 示例 expected=19 过期；参数表 2036→18.5，引擎按表取 18.5 正确 |
| 医保已缴120月，要求180月 -> 缺60月 | 示例缺 gender 输入且引用已废弃参数；引擎正确追问性别 |
| 断缴2个月 | R-300 复用自然月差 builtin 与"未覆盖整月数"语义差 1；共享逻辑被 R-120+单测锁定，正解需规则变更+口径确认（待办） |
| 下限基数5000，比例0.3，补贴50% -> 750 | 浮点表示噪声（1500.0000000000002），数值正确，属展示层取整问题 |

快照机制同时锁定这 4 条偏差的"实际输出"：任何一条变化（新回归或有意修复）都会使快照测试变红，强制人工复核。

## 5. 验证证据

| 验证 | 结果 |
| --- | --- |
| 重复执行零漂移断言 | 通过（全新对象两次执行 canonical JSON 完全一致） |
| 快照一致断言 | 通过（28/28 条与提交基线一致） |
| `npm test` | 退出码 0；16 文件、114 测试（新增 1 文件 2 测试） |
| `npx eslint src` | 退出码 0；无输出（修复了夹具初版引入的 1 个 unused-vars 告警） |
| `npx tsc --noEmit` | 退出码 0 |

## 6. 结论

FND-FR-003 满足：测试夹具覆盖规划输入/输出/trace 与已知偏差；重复执行 plan/calc/trace 无未解释漂移。步骤01.2 完成。
