# 步骤 01.6 CI 门禁报告

- 需求：FND-FR-009
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 交付物

- `.github/workflows/ci.yml`：GitHub Actions 门禁流水线（push + PR 触发），步骤：
  1. `npm ci`（锁定依赖安装）
  2. `npx eslint src`（源码 Lint——本地评估目录不在 `src/` 且不入库，天然排除）
  3. `npx tsc --noEmit`（类型检查）
  4. `npm test`（单元 + 黄金 + 快照 + 契约测试）
  5. `npm run build`（Next 生产构建，占位配置）
  6. `node scripts/scan-secrets.mjs --all`（全库 Secret 扫描，FND-FR-010）
- 排除规则写入工作流注释：构建使用占位环境变量（DATABASE_URL/NEXTAUTH_*/ADMIN_*/OPENAI_* 占位值），真实密钥只存在部署环境（PRD §9）。

## 2. 干净环境验证（模拟 CI）

方法：将仓库 clone 到仓库外临时目录（clone 后确认**不存在** `.env`/`.env.local`——密钥不入库的独立证据），在占位配置下执行全套门禁命令：

| 命令 | 退出码 | 备注 |
| --- | --- | --- |
| `npm ci` | 0 | lockfile 完整安装 |
| `npx eslint src` | 0 | |
| `npx tsc --noEmit` | 0 | |
| `npm test` | 0 | 全部测试通过 |
| `npm run build` | 0 | **无 .env、无真实密钥、未连数据库**——FND-AC-004 证据 |
| `node scripts/scan-secrets.mjs --all` | 0 | 220 个跟踪文件零命中 |

验证后临时 clone 已删除，不污染仓库候选。

## 3. 结论

FND-FR-009 满足：CI 步骤与排除规则固化，全套门禁在干净环境新鲜执行且全部通过。步骤01.6 完成。
