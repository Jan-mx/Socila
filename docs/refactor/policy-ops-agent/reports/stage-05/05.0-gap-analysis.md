# 步骤 05.0 Gap Analysis（恢复校准，2026-08-30）

对照：ADR-0002（Personal Demo）、ADR-0003（远程OCR）、ADR-0006（原生解析优先）、operational-baseline.md、quality-gates.md。

## 已就位（测试通过）

| 项 | 证据 |
| --- | --- |
| 来源白名单 + SSRF/私网/重定向/大小防护 | `test_rag_core.py` 8/8 |
| 格式适配器（MD/HTML/JSON/DOCX/XLSX→DocumentTree） | 同上 |
| 父子/表格分片 + 稳定 Chunk ID | 同上 |
| 摄取状态机 + 哈希去重（AC-005）+ 三层落库（AC-001） | `test_rag_integration.py` 2/2 |
| 混合检索（FTS+向量+RRF+rerank+父回填）+ 地区/日期过滤 + 审计 | 同上 |
| 演练环境 | pgvector/pg17 + Redis + MinIO 三容器 healthy |

## 缺口与处置（TDD 补齐顺序）

| # | 缺口 | 处置 |
| --- | --- | --- |
| G1 | Worker 统一配置（memory 768MB/软640MB、max_tasks_per_child=20、task_timeout 120s） | celery conf 补齐（ADR-0002/operational-baseline） |
| G2 | PDF 逐页（PyMuPDF 原生文本 + 页面哈希 + 页数≤200） | uv add pymupdf；`rag/pdf.py` + 测试 |
| G3 | OCR-VL 推理客户端（ADR-0003）+ OcrDiscrepancy→needs_correction + 页面哈希缓存 | `rag/ocr.py` + Fake/真实双实现 + 测试 |
| G4 | XLSX 10万行、JSON>5MB 流式（ijson）、lxml 禁外部实体 | 适配器补齐 + 测试 |
| G5 | 05.7 SiliconFlow 真实验证（/models、/embeddings、/rerank、OCR-VL 推理；公开样本；只记状态/维度/用量/trace） | 复用 validate-siliconflow 流程 + Python 真实请求，报告落 config/ |
| G6 | 05.10 评测 harness + 黄金集（Precision≥0.85/Recall≥0.90/引用100%/错地区=0） | 小型标注集 + 指标计算 |
| G7 | 05.11 验收报告 + 阶段提交 | 模板化 |

## 明确不做（ADR-0002/0006 边界）

- Demo 服务器不常驻 Docling 完整流水线/本地 OCR/Playwright。
- `.doc`/`.xls`/超限文件转开发机离线处理。
- 不降低 quality-gates 任何阈值。
