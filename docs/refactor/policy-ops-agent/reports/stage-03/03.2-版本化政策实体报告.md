# 步骤 03.2 版本化政策实体报告

- 需求：POL-FR-003～006
- 日期：2026-08-30
- 分支：`refactor/policy-ops-agent-platform`

## 1. 交付物

| 交付物 | 位置 |
| --- | --- |
| migration 0004 | `drizzle/0004_policy_versioning.sql`：rules/params 增加 `jurisdiction_code`+`business_key`（唯一索引 (地区,业务键,版本)）、params 增加 `effective_to`、rule_sets/tests/policy_pack_versions 增加地区列、policy_pack_versions 增加 `pack_kind`(baseline/overlay CHECK)、规则与参数有效期 CHECK 约束、现网行回填（上海 310000，business_key=业务标识） |
| Schema 同步 | `src/lib/db/schema.ts`（5 表 + params.effectiveTo） |
| 领域不变量 | `src/server/modules/policy/domain/invariants.ts`：validateInterval、intervalsOverlap、detectOverlappingVersions（同级同键同状态重叠检测——03.4 冲突模型核心） |
| 测试 | `policy/__tests__/invariants.test.ts`（3 项：倒挂区间拒绝、开区间语义、跨地区隔离的重叠检测） |

迁移事故记录：初版 0004 缺少 `--> statement-breakpoint` 分隔符且 params 缺 `effective_to` 列，导致 drizzle migrator 单事务批跑整体回滚（首次空库重建失败）；修复后空库重建成功。

## 2. 验证证据

| 验证 | 结果 |
| --- | --- |
| 空库重建（0000~0004） | `migrations applied`，jurisdictions 4 行 |
| 回填 | seed 后 rules/params 全部带 `jurisdiction_code=310000` + `business_key`（首次 seed 时回填生效） |
| 不变量测试 | 3/3 通过 |
| `npm test` 全套 | 25 文件：24 通过 + 1 skipped；151 通过 |

## 3. 结论

POL-FR-003～006 的数据模型与领域不变量就位。步骤03.2 完成。
